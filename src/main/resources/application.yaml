spring:
  datasource:
    url: ${TO_DO_LIST_PG_URL:jdbc:postgresql://localhost:5432/todolist}
    username: ${TO_DO_LIST_PG_USER:postgres}
    password: ${TO_DO_LIST_PG_PWD:230705}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true